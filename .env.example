# Almanac's actual environment-variable contract (fixes INVENTORY.md
# AUTH-1: the old file, auto-generated from whatever Infisical held,
# listed 10 keys while the code only ever read 3).
#
# In normal operation none of this is read from a file at all: Latch
# injects these directly into the process (AR8) —
#   latch run -- ./target/release/almanac
# This file exists only as a reference for what the three variables
# are, and as a convenience for manually exporting them during local
# development without Latch.

# Service account email — becomes the JWT "iss" claim.
CLIENT_EMAIL=

# RSA private key, PEM format, from the Google service-account JSON.
PRIVATE_KEY=

# Google's OAuth2 token endpoint. Almost always:
TOKEN_URI=https://oauth2.googleapis.com/token
